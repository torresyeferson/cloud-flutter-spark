import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.puntualometro',
  appName: 'Puntualometro',
  webDir: 'dist',
};

export default config;
